#include <stdio.h>

/**
 * main - prints ASCII value of characters
 *
 * Return:  0.
 */
int main(void)
{
	char c = 'c';
	char a = '1';
	char b = '!';

	printf("The ASCII value of %c, %c, %c is: %d, %d, %d\n", c, a, b, c, a, b);
	return (0);
}

